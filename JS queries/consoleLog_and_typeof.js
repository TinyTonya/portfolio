let engine = '249 hp';
let turbo = true;
let maxSpeed = 260;
console.log(engine, turbo, maxSpeed); // выведи переменные engine, turbo и maxSpeed
console.log(typeof engine, typeof turbo, typeof maxSpeed); // выведи типы переменных engine, turbo и maxSpeed